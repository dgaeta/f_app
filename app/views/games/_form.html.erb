<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :creator_id %><br />
    <%= f.number_field :creator_id %>
  </div>
  <div class="field">
    <%= f.label :creator_first_name %><br />
    <%= f.text_field :creator_first_name %>
  </div>
  <div class="field">
    <%= f.label :is_private %><br />
    <%= f.check_box :is_private %>
  </div>
  <div class="field">
    <%= f.label :duration %><br />
    <%= f.number_field :duration %>
  </div>
  <div class="field">
    <%= f.label :wager %><br />
    <%= f.number_field :wager %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
