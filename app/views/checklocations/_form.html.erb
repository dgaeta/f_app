<%= form_for(@checklocation) do |f| %>
  <% if @checklocation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@checklocation.errors.count, "error") %> prohibited this checklocation from being saved:</h2>

      <ul>
      <% @checklocation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :requester_id %><br />
    <%= f.number_field :requester_id %>
  </div>
  <div class="field">
    <%= f.label :gym_name %><br />
    <%= f.text_field :gym_name %>
  </div>
  <div class="field">
    <%= f.label :geo_lat %><br />
    <%= f.text_field :geo_lat %>
  </div>
  <div class="field">
    <%= f.label :geo_long %><br />
    <%= f.text_field :geo_long %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
